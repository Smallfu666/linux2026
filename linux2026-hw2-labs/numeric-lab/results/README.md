# Sample Results

These files are small, checked-in examples of the demo scripts' output.

- `binary_fraction.txt`
- `radix_economy.csv`
- `balanced_ternary.csv`

Validation status on 2026-05-04:

- these sample outputs were regenerated in WSL2 Linux x86_64
- `dot` is not installed in this WSL image, so the optional Graphviz render steps were not exercised here

Regenerate them by running the matching script from `numeric-lab/` and redirecting stdout into the corresponding file.
