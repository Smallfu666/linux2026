# Results

Generated result files for the dispatch benchmark go here.

Typical outputs:

- `local_elapsed.csv` from `run.sh`
- `perf_switch.txt` for Linux `perf stat` runs of `./bench_switch`
- `perf_cgoto.txt` for Linux `perf stat` runs of `./bench_cgoto`
