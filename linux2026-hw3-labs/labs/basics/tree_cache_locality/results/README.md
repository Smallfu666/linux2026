# Results

Generated result files for the tree locality benchmark go here.

Typical outputs:

- `local_elapsed.csv` from `run.sh`
- `perf_bst.txt` for Linux `perf stat` runs of `./bench --type=bst`
- `perf_btree16.txt` for Linux `perf stat` runs of `./bench --type=btree16`
